# Dataverse MCP Server Configuration

# Dataverse Environment URL (required)
# Example: https://org1bfe9c69.api.crm.dynamics.com
DATAVERSE_HOST=https://your-org.api.crm.dynamics.com

# Azure AD Tenant ID (required)
# Find: Azure Portal → Azure Active Directory → Overview → Tenant ID
DATAVERSE_TENANT_ID=your-tenant-id-here

# App Registration Client ID (required)
# Find: Azure Portal → Azure Active Directory → App Registrations → Your App → Application (client) ID
DATAVERSE_CLIENT_ID=your-client-id-here

# App Registration Client Secret (required)
# Create: Azure Portal → Azure Active Directory → App Registrations → Your App → Certificates & secrets
DATAVERSE_CLIENT_SECRET=your-client-secret-here

# Optional: MCP Server Name (defaults to 'databricks-mcp')
# SERVERNAME=dataverse-mcp-server

# Optional: Databricks Configuration (if deploying to Databricks Apps)
# DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
# DATABRICKS_TOKEN=your-pat-token

